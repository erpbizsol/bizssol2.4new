import { PanValidatorDirective } from './pan-validator.directive';

describe('PanValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PanValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
